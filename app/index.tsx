import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView className="flex-1 w-screen items-center justify-center bg-white0">
      <View className="">
        <Text>Home</Text>
      </View>
    </SafeAreaView>
  );
};

export default Home;
